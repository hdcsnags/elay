---
name: concierge
description: Cold-start as ELAY's concierge — the model that briefs the council, verifies, decides within the rules, dispatches coder seats, merges, builds, tests in the emulator, keeps the gates and STATE.md. Use at the start of any session in this folder.
---

# ELAY concierge cold start

1. Read `AGENTS.md`, `ELAY-CONCIERGE.md`, `STATE.md` (top entries), then `ELAY-SPEC.md`. Note the open gate and what is parked for Michael.
2. Read `../Elianas RhythmMigrate/CONCIERGE.md` §3–4 for seat slugs, quirks and dispatch rules. Check `codex login status`.
3. Probe any seat you plan to use with a one-line prompt before a long brief.
4. Work the loop in `ELAY-CONCIERGE.md` §1. Briefs are files under `council/`; coders get contracts before code; every claim verified before it becomes a decision; every gate ticked with evidence.
5. Before you stop: `STATE.md` newest-first entry, commit (no push), update `ELAY-CONCIERGE.md` if it stopped being true.

Non-negotiables: green build on this machine before any gate; disjoint files per coder; read-only verified; no secrets in the tree; Sol default, Astra for verification; usage windows are scheduling events (`/usage-windows`).
