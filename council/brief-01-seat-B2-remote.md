Coder seat B2 — ELAY Phase 1: supabase-kt adapters + the outbox replay engine. You have WRITE access in your own clone; your diff is collected for concierge review.

READ FIRST: `contracts/phase1-planner.md` §2 (RPC semantics), §3 (replay contract — binding), §4 (wire formats), §6 (your paths); the frozen interfaces `dev/elay/data/remote/Gateways.kt` and `dev/elay/sync/SyncContracts.kt`; seat A's fixtures in `contracts/fixtures/*.json` (canonical wire rows — your DTOs must round-trip them byte-comparably); the Room layer in `dev/elay/data/local/` (OutboxDao, PlannerWriteDao — already merged); `adr/ADR-002` (thin-adapter rule: supabase-kt types never leak past your impl package).

BUILD, only under `shared/src/commonMain/kotlin/dev/elay/data/remote/dto/`, `shared/src/commonMain/kotlin/dev/elay/data/remote/impl/`, `shared/src/commonMain/kotlin/dev/elay/sync/impl/`, and commonTest equivalents:
1. **DTOs** (`data/remote/dto/`): @Serializable per entity, @SerialName snake_case matching the fixtures exactly; instants as ISO-8601 UTC strings; enums as wire strings; explicit mappers DTO↔domain (reuse domain enums' `wire`).
2. **`SupabaseDataGateway`** (`data/remote/impl/`): implements DataGateway; constructor takes a provided `SupabaseClient` (do NOT construct one — DI is concierge wiring); each method calls the matching `rpc_*` via postgrest rpc with `p_operation_id/p_expected_version/p_row`, parses `{"outcome":"applied"|"conflict",...}` into MutationResult; network/HTTP errors → `Failed(reason, retryable=true)` for IO/5xx/429, `retryable=false` for 4xx; never throws.
3. **`SupabaseAuthGateway`**: implements AuthGateway over supabase-kt Auth (email/password only — Phase 1 local test accounts); session Flow from the SDK's session status; refreshSession delegating to the SDK.
4. **`OutboxSyncCoordinator`** (`sync/impl/`): implements SyncCoordinator per contract §3 EXACTLY — strict FIFO per aggregateId, one in flight globally, full-jitter backoff base 1s factor 2 cap 120s persisted via OutboxDao.mark/attempts, auth-pause, terminal FAILED on non-retryable, CONFLICT holds row + stores server row (extend nothing in data/local — if you need a new column/DAO method you may NOT add it; instead surface the need in your report), Applied → PlannerWriteDao atomic ack. Inject a clock/delay function so tests control time.
5. **Tests** (commonTest): fixture round-trips per entity (read `contracts/fixtures/<e>.json` content — embed the JSON strings as test constants copied verbatim; files aren't readable from commonTest resources yet — note this as a limitation); replay-engine tests against a scripted fake DataGateway: FIFO order kept, retry/backoff sequence, conflict path, idempotent double-ack safe, auth-pause path. Do not hit the network in tests.

TOUCH ONLY the paths above. Never: gradle files, data/local/ (read-only dependency), domain/, ui/, contracts/, .github/. supabase-kt deps are already wired.

VERIFY inside the clone: `export JAVA_HOME='C:\Program Files\Android\Android Studio1\jbr'` then `./gradlew.bat :shared:allTests :androidApp:assembleDebug ktlintCheck detekt --console=plain --no-daemon` — BUILD SUCCESSFUL required; report executed-test count (baseline 56) and delta. ktlint nits: `./gradlew.bat ktlintFormat --no-daemon` then re-run. Run verification SYNCHRONOUSLY in the same tool call — no background runs, no monitors.

Report: file list · verification commands + final lines · test count/delta · any data/local extensions you needed but could not make · UNVERIFIED items. Begin with the model you are running as.
