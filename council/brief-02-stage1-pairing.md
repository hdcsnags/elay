Sol — you hold the contracts + adversarial verification lane (master-plan-v2). This is the STAGE 1 CONTRACT round: pairing + realtime events for a recurring pair. Read-only; your output becomes `contracts/stage1-pairing.md` after the lead's review, so write the load-bearing sections as if they ship.

CONTEXT. Read: `council/master-plan-v2.md` (pairs-first pivot — PAIRS ONLY, no groups), `ELAY-SPEC.md` §7 (households schema shape), §8 (authz), §2 (Together surface), `adr/ADR-007` (visibility + field projection), `adr/ADR-009` (revocation), `adr/ADR-010` (commitments — Stage 2 consumes this), the shipped migrations in `supabase/migrations/` (five personal tables carry a Phase-1 guard `household_id is null and visibility='private'` that Stage 1 must migrate away), and `contracts/phase1-planner.md` (house contract style, RPC/receipt pattern, wire rules). Constraint from the lead: real FCM push is account-blocked (Firebase project needs Michael) — Stage 1 uses **Supabase Realtime** for event delivery; design push-ready but realtime-first.

THE CONTRACT TO WRITE — sections, in the house style:
1. **Schema**: `pairs` (or households constrained to 2 — argue which and commit), `pair_members`, `pair_invites` (expiring, single-use, code-based — a short human-typeable code since web RSVP is Stage 3), with full CHECKs, RLS (invite redemption must not leak the inviter's data pre-acceptance), grants, and the **guard-drop migration**: replace the Phase-1 private-only CHECK with real visibility semantics (owner-only RLS stays; member-visible reads ONLY via the ADR-007 safe projections — define the first projection views/RPCs for shared objects).
2. **RPCs + receipts**: `rpc_create_pair_invite`, `rpc_redeem_pair_invite` (atomic: validate code/expiry/unused, create membership, single pair per user in V1 — argue if that's wrong), `rpc_leave_pair` (ADR-009 revocation semantics), each idempotent via `mutation_receipts`, conflict-safe, with pgTAP case lists (allow/deny/replay/expiry/double-redeem/self-redeem).
3. **Realtime contract**: channel naming (`pair:<id>`?), which events flow (member joined/left; Stage-2 proposal events reserved), authorization (RLS on realtime — no channel-name guessing leaks), client subscription lifecycle (user-scope owned).
4. **Client surface contract**: the frozen Kotlin interfaces Stage-1 seats build against — `PairRepository` (observePair, invite code create/redeem, leave), DTO wire shapes, and what Together renders in Stage 1 (pair status + invite flow + member joined empty-state; NO shared objects yet — that's Stage 2 with time locks).
5. **Seat slicing** for the lead: disjoint paths for A2 (SQL), B3 (client data), C2 (Together UI + Task/Goal detail catch-up from the deferred list), what the lead wires.
6. **Adversarial cases** the pgTAP suite must include, and the 3 most likely agent failure modes for THIS stage with guardrails.

Ground in the files (quote §s and migration filenames). Read-only: modify nothing. 1,200–1,800 words. Begin with the model you are running as. End with JSON between BEGIN VERDICT / END VERDICT:
{"pair_model":"dedicated_pairs_table|households_capped","single_pair_per_user":true|false,
 "invite":"code|link|both","guard_drop":"one_migration|phased","sections_ready":true|false,
 "seat_slices":[{"seat":"...","paths":["..."]}],"failure_modes":[{"risk":"...","guardrail":"..."}],"confidence":0.0-1.0}
