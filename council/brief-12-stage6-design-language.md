Michael here, via Fable (lead orchestrator; build authority delegated). STAGE 6 — THE DESIGN LANGUAGE. Michael has ruled the brand direction (2026-09-23), and it changes the bar for this stage entirely:

**The name ELAY is deeply personal to Michael — treat it with the care of a name that matters. The quality bar he set, verbatim in spirit: the app must NOT "look like a man built it." The judge is a person with a genuinely high fashion sense. The test for every choice in this stage: would a discerning, fashion-literate woman choose to keep this app on her phone and show it to people?**

What that means (and does not mean): it does NOT mean "make it pink" or any gendered cliché — a fashion-literate judge spots pandering instantly and it fails harder than default Material. It means TASTE: editorial typography with real hierarchy; a restrained, confident palette (think fashion-house restraint, not tech-brand saturation); generous whitespace; considered motion; empty states with warmth instead of gray boxes; an app icon that looks like a brand, not a template. Reference class: apps that win Apple Design Awards for elegance (Gentler Streak, Things, Flighty-class polish), editorial products (vogue-adjacent restraint), premium journaling/wellness apps. ELAY is a planner for TWO PEOPLE who care about each other — the design should feel intimate and warm, never corporate, never gamified.

Current honest state (all in research/shots/): stock Material3 with a faint purple tint, the DEFAULT ANDROID ROBOT as launcher icon, gray cards, raw-seconds timestamps in Plan, flat list layouts, no dark theme verification, no motion, no brand anywhere. Read STATE.md's closure entries for the tracked cosmetic debt (bare "UTC" rows, composer scaffold titles, Plan seconds, icon substitutions C7 flagged).

Two blind lanes. Read-only; modify nothing; deliver the COMPLETE lane in one final message — never end "awaiting". Begin with the model you are running as.

CONTEXT (both lanes): `ELAY-SPEC.md` §2 (calm ops voice — the design must embody it visually), the §B lane docs from stages 2/4/5 (`council/stage2-timelock-gemini.md` etc. — the copy voice already shipped), `adr/` (ADR-004 mentions the deferred token layer), the current UI code under `shared/src/commonMain/kotlin/dev/elay/ui/` (theme setup, if any, and how screens compose today), `research/shots/stage4-e2e-01-plan-gauge.png` + `stage5-e2e-01-today-wrapup.png` + `stage3-e2e-02-share-sheet.png` (what it looks like NOW), and the constraint set: Compose Multiplatform 1.11.1, Material3 as the base system (we THEME it, we don't replace it), phone-first, both light AND dark themes required this time.

§A — DESIGN SYSTEM CONTRACT (the Claude/Opus seat): the buildable architecture:
1. Token layer (ADR-004 finally): the exact Kotlin shape — color tokens (semantic, not literal: `surfaceWarm`, `accentPrimary`, `textEditorial`…), type scale (display/headline/title/body/label with the editorial hierarchy §B will specify), spacing/radius/elevation scales, motion durations/easings — as a `ui/theme/` package feeding a customized `MaterialTheme` (colorScheme + typography + shapes) so EXISTING screens inherit most of it for free; identify the places where Material defaults will still leak (component-level overrides needed) by reading the actual screens.
2. Light + dark scheme derivation rules from one palette; contrast floors (WCAG AA minimum, name the ratios).
3. The app icon + splash pipeline in code: adaptive icon (foreground vector drawable + background), monochrome layer for themed icons (Android 13+), splash screen API alignment — spec the deliverable files exactly (`ic_launcher` adaptive XML set, the vector's construction) so a coder seat can produce them from §B's design.
4. Typography implementation reality: which font families are shippable (bundled font resources in CMP — name the mechanism and size budget; Google Fonts downloadable vs bundled trade-off; a system-stack fallback rule).
5. Migration plan for the shipped screens (Today/Plan/Together/Inbox/composer/sheets/web RSVP page) — orderable slices for coder seats with disjoint grants, riskiest visual regressions named; the web RSVP page's inline CSS gets the same tokens (name the mechanism — shared values doc, since the edge function can't import Kotlin).
6. What stays OUT of scope (no gamification, no illustration system yet unless §B's empty states demand a minimal one, no rebrand of copy voice), + 3 failure modes as "X is forbidden BECAUSE Y; Z remains required."
End §A: BEGIN VERDICT/END VERDICT {"lane":"design-system-contract","token_home":"ui/theme/","fonts":"bundled|downloadable|system","sections_ready":true|false,"seat_slices":[...],"failure_modes":[...],"confidence":0..1}

§B — THE DESIGN LANGUAGE ITSELF (the agy/Gemini seat): the taste. This is the stage's soul — be SPECIFIC, not vibes:
1. Palette: exact hex values, light + dark, with the reasoning (what feeling each carries); the accent story (one confident accent or a warm duo — argue); how the pair's two-person-ness shows up chromatically (e.g., a subtle two-tone motif for "you" vs "them" in dual-time displays — or argue against it).
2. Typography: name real families (available via Google Fonts for bundling — e.g. editorial serif for display + humanist sans for body, or a single superfamily — argue and CHOOSE), the full scale with sizes/weights/letter-spacing, and exactly where the editorial voice appears (screen titles? the dual-time line? numbers?).
3. Shape + space + elevation language: radii, the card language replacing today's gray slabs, list rhythm, where whitespace does the work borders used to.
4. Motion: 3–5 signature moments ONLY (the realtime card flip deserves one; the "On both plans" confirmation; the wrap-up card retiring) — durations/easings/what moves; everything else stays instant and calm.
5. The icon: describe the adaptive app icon precisely enough to draw as a vector (a monogram/mark concept honoring the name ELAY — elegant, not techy; describe geometry, weight, background treatment, monochrome variant), plus the splash moment.
6. Empty states + the small shames: the exact treatment for today's raw-seconds Plan rows, bare "UTC" rows, gray empty cards — each gets its designed replacement described concretely.
7. Dark theme as a first-class look (not inverted light) — describe its distinct mood.
8. A "taste test" checklist the verifier can apply to screenshots: 8–12 binary checks that distinguish this design executed-well from executed-poorly.
End §B: BEGIN VERDICT/END VERDICT {"lane":"design-language","palette_anchor":"<hex>","display_font":"...","body_font":"...","signature_motions":[...],"copy_ready":true|false,"confidence":0..1}

Ground §A in the actual code and §B in the actual screenshots. Each lane 1,200–1,800 words.
