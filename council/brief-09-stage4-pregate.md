Michael here, via Fable (lead orchestrator; build authority delegated). STAGE 4 PRE-GATE VERIFICATION — honest availability. Adversarial verification before closure. Read-only; modify nothing; STATIC pass except you MAY run `npx supabase test db --local` and rolled-back psql repros via `docker exec supabase_db_Elay psql -U postgres -d postgres -c "begin; ...; rollback;"`. No Gradle/emulator/functions-serve. Deliver COMPLETE numbered findings + verdict JSON in one final message. Begin with the model you are running as.

CLAIMS UNDER TEST (all the lead's — attack them):
1. Contract compliance: `contracts/stage4-honest-availability.md` (+ §A guardrails) vs `supabase/migrations/20260912200000_stage4_honest_availability.sql` + pgTAP `0022` (613/613 claimed). Attack especially: the certainty ladder's busy-always-wins and freshness/coverage math (build stale/non-covering repros), the coalescing (adjacent-and-overlapping → single window; can interval COUNT still fingerprint sources in any case?), peer-leak vectors in every projection (label! source_tag, synced_at, external_ref_hash), the true-upsert lead fix (guessed-id/cross-owner semantics, the 23505 path), fn_sync_external_busy atomicity, the shared attempts limiter (can one RPC starve the other? is the opaque over-limit shape actually identical?), and the RESEQUENCE claim that hints now honestly reflect external busy in `rpc_proposal_conflict_hints` for the PARTNER.
2. A6's ratified judgment calls: manual-source existence upgrading free_per_elay→free_per_calendar (is that honest labeling? a user with only a manual source gets "free per calendar" — argue), one-fresh-source-suffices, manual TTL 30d.
3. Client (B7/C6): mapping inside the failure barrier; unknown-certainty tolerance; C6's request-id guard against stale hint responses (read TogetherProposalViewModel — can a stale response still land?); the composer refresh cadence (does every candidate edit fire an RPC — rate-limit interaction with the 60/15min limiter for a fidgety user?); DI wiring correctness (per-user scope, no leak on sign-out — AvailabilityRepository has no close(); is that right?).
4. Evidence honesty: STATE newest entry vs shots `stage4-e2e-01..03` (the composer's own label is NOT visibly captured — only the responder chip badge; is the claim worded honestly?).
5. Regression sweep 0001–0021 + fresh-reset convergence (the live DB got one lead function patch — the true-upsert fix; verify the FILE carries it; run pgTAP yourself).

DELIVERABLE: numbered findings tagged VERIFIED-OK / BUG (repro) / RISK (exploit sketch) / UNVERIFIED, file:line quotes, then JSON between BEGIN VERDICT/END VERDICT:
{"lane":"pregate-verification","stage4_ready_to_close":true|false,"blocking_findings":[...],"nonblocking_findings":[...],"confidence":0..1}
