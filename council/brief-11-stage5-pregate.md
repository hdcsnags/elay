Michael here, via Fable (lead orchestrator; build authority delegated). STAGE 5 PRE-GATE VERIFICATION — retention + hardening, the FINAL stage gate of master-plan-v2. Adversarial verification before closure. Read-only; modify nothing; you MAY run `npx supabase test db --local` and rolled-back psql repros via `docker exec supabase_db_Elay psql -U postgres -d postgres -c "begin; ...; rollback;"`. No Gradle/emulator/functions-serve. Deliver COMPLETE numbered findings + verdict JSON in one final message. Begin with the model you are running as.

CLAIMS UNDER TEST (all the lead's — attack them):
1. Retention SQL: `contracts/stage5-retention-hardening.md` + §A guardrails vs `supabase/migrations/20260912210000_stage5_retention_hardening.sql` + pgTAP `0023` (700/700 claimed). Attack: the privacy ruling (build a repro proving the PEER can never read/derive an outcome — incl. through every pair projection and the suggestion RPC), the elapsed-only guard, upsert/version semantics, the suggestion honesty threshold (sample_size>=2, last-5 median — boundary repros), title-vs-task matching edge cases (case, whitespace), A7's 7 flagged judgment calls (esp. "LAST FIVE by block starts_at DESC" and the full-CRUD table grants vs Stage 4's zero-write-grant pattern — is direct UPDATE/DELETE by the owner safe alongside the RPC's receipts?).
2. Hardening delta on the FILES: AndroidManifest(+debug overlay), the three res/xml files (is the debug NSC override complete? does main's NSC break anything?), proguard-rules.pro (any missing keep class you can identify statically), build.gradle.kts (the signing-if-present block — can a missing property crash config? the endpoint assertion + escape hatch semantics), detekt println ban (any println left in commonMain/androidMain?), .gitignore additions, docs/release.md accuracy.
3. ElayLog: release-strippedness (the enabled flag path — can anything log before ElayApp sets it? is iOS's actual honest?), the two content fixes (auth class-name-only, hashed topics — grep for any remaining raw-topic or error-message logging).
4. Retention UI claims vs evidence: STATE's newest entry vs shots `stage5-e2e-01..07` (distinct md5s! filename-content match! — the Stage-4 lesson), the version-2 upsert claim, the minified-smoke claim (shots 06/07 show sign-in + Today under the scratch-signed release APK).
5. Regression: 0001–0022 + fresh-reset convergence (live DB vs migration file for all Stage-5 functions); the C7 "Right on time"→finished_early(delta 0) fold — does it corrupt the suggestion median?
6. The master plan: with Stage 5 closing, audit `council/master-plan-v2.md`'s promised scope vs shipped reality — list anything promised-but-not-shipped that isn't already a named deferral (PING/contract amendments).

DELIVERABLE: numbered findings tagged VERIFIED-OK / BUG (repro) / RISK (exploit sketch) / UNVERIFIED, file:line quotes, then JSON between BEGIN VERDICT/END VERDICT:
{"lane":"pregate-verification","stage5_ready_to_close":true|false,"blocking_findings":[...],"nonblocking_findings":[...],"confidence":0..1}
