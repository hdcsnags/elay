Michael here, via Fable (lead). Stage 6 seat **D1 — the token layer**. You are a coder seat in a scratch clone. Contract: `contracts/stage6-design-language.md` (FROZEN — read it first, then `council/stage6-design-opus.md` §§1–2 for the architecture and `council/stage6-design-gemini.md` §§1–3 + §7 for every hex/size/radius). LEAD-0 is already merged (splash theme, core-splashscreen, the `assertColorTokenDiscipline` rail).

## Grant (disjoint — touch NOTHING else)
- `shared/src/commonMain/kotlin/dev/elay/ui/theme/**` (rewrite `ElayTheme.kt`; create the rest)
- `shared/src/commonTest/kotlin/dev/elay/ui/theme/**` (create `ElayContrastTest.kt` + any token unit tests)

**No screen edits.** Screens migrate in later slices; your job is that the moment this merges, every `MaterialTheme.colorScheme.*` / `.typography.*` / `.shapes.*` read in the existing 44 UI files lands on ELAY values.

## Deliverables (§A §1 is the spec; §B is the value source)
1. `ElayColors.kt` — `@Immutable data class ElayColorTokens` with semantic names: `surfacePaper`, `surfaceRaised`, `surfaceSunken`, `hairline`, `hairlineFaint`, `textEditorial`, `textPrimary`, `textMuted`, `accentPrimary`, `onAccentPrimary`, `accentContainer`, `onAccentContainer`, `accentQuiet`, `availabilityCalm`, `availabilityCaution`, `onAvailabilityCaution`. NO `youTint`/`themTint` (contract amendment 1 — §B rejected chromatic pair identity). `elayLightColors()` / `elayDarkColors()` from §B's §1 table EXACTLY:
   - Light: surface `#FBF9F6`, raised `#F3EFEA`, sunken `#EBE5DD`, text `#1C1917`, muted `#68625D`, hairline `#DCD6CD`, faint `#EAE5DE`, accent `#6D3240`, onAccent `#FFFFFF`, accentContainer `#F4EAEB`, onAccentContainer `#3D121E`, caution `#8C6541`, onCaution `#FFFFFF`.
   - Dark: surface `#171415`, raised `#211D1F`, sunken `#2B2628`, text `#F4EFEA`, muted `#A8A19B`, hairline `#3E373A`, faint `#2A2426`, accent `#DF9EAC`, onAccent `#441926`, accentContainer `#4F2633`, onAccentContainer `#FAD8DF`, caution `#E0B286`, onCaution `#462A0D`.
   - `availabilityCalm`: derive a quiet sage/stone green that PASSES the contrast floors on both raised surfaces (you choose the hex; it replaces nothing yet).
   - `LocalElayColors` + **`fun ElayColorTokens.toMaterialColorScheme(dark: Boolean): ColorScheme`** — map every M3 slot the census reads (surface, onSurface, onSurfaceVariant, surfaceVariant, surfaceContainer\*, primary/onPrimary/primaryContainer/onPrimaryContainer, secondary→muted taupe family, tertiary→Cedar caution, outline/outlineVariant, background, error stays M3 default red family adjusted for warmth if trivial). Unmapped slots take the nearest semantic token, never M3 baseline purple.
2. `ElayType.kt` — `elayTypography(f: ElayFontFamilies): Typography`, all 15 roles per §B §2's table (Newsreader display/headline/titleLarge-italic; Plus Jakarta Sans the rest; sizes/lineHeights/weights/tracking as tabled; fill the unused display*/headlineLarge slots per the same editorial logic). Until D2 lands, `ElayFontFamilies` defaults every family to `FontFamily.Default` — the structure ships now, the faces swap in via D2.
3. `ElayShapes.kt` — `ElayRadius` (card 20, tile 14, field/stepper 12, sheet 28, pill full) + `elayShapes: Shapes`.
4. `ElaySpacing.kt` — `@Immutable data class` (hair 2, xs 4, sm 8, md 12, lg 16, xl 24, xxl 40, gutter 20, touchMin 48, hourHeight 56 — all `Dp`) + `LocalElaySpacing`. `hourHeight` is layout math (Plan timeline), not decoration.
5. `ElayElevation.kt` — levels as Dp + the dark rule (raised = lighter surface step, never shadow); tonal elevation constant `0.dp`.
6. `ElayMotion.kt` — `instant=0, quick=120, standard=220, wrapUpCollapse=320, expressive=400, deliberate=650` (ms, Int), `easeEditorial = CubicBezierEasing(0.2f, 0f, 0f, 1f)`, `easeExit = CubicBezierEasing(0.4f, 0f, 1f, 1f)`, `LocalElayMotion`.
7. `ElayFonts.kt` — `data class ElayFontFamilies(display, displayItalic, body: FontFamily)` + `@Composable fun elayFontFamilies(): ElayFontFamilies` returning defaults (D2 replaces the body of this one function ONLY — keep it isolated).
8. `ElayTheme.kt` — rewritten: `@Composable fun ElayTheme(darkTheme: Boolean = isSystemInDarkTheme(), content: @Composable () -> Unit)`; provides `LocalElayColors/ElaySpacing/ElayMotion` via `CompositionLocalProvider` wrapping `MaterialTheme(colorScheme = tokens.toMaterialColorScheme(darkTheme), typography = elayTypography(elayFontFamilies()), shapes = elayShapes)`.
9. `ElayComponents.kt` — thin wrappers/named overrides over PUBLIC `*Defaults` APIs only (§A's leak list): `ElayCard` (raised fill, hairline border option, 20dp radius, 0 elevation), `elayNavigationBarItemColors()`, `elaySheetDefaults` (container/scrim/drag-handle colors), `elayFilterChipColors()`, `elayTextFieldColors()`, `elayButtonShapes/padding`. These are ADDITIVE — no existing call sites change in this slice.
10. `commonTest .../theme/ElayContrastTest.kt` — pure Kotlin WCAG relative-luminance math over an EXPLICIT pair table for BOTH schemes: text pairs ≥ 4.5:1 (textPrimary/textEditorial on all three surfaces; onAccent on accent; onAccentContainer on accentContainer; onCaution on caution), muted text ≥ 4.5:1 on paper+raised, non-text ≥ 3:1 (hairline vs the surfaces it separates is EXEMPT if you document it; accent vs surface, caution vs surface ≥ 3:1). If a §B hex fails a floor, DO NOT silently change it — adjust the failing hex minimally (same hue family), note the delta in a `## Deviations` section of your final message, and make the test pin the shipped value.

## House rules
- ktlint/detekt must pass: run `export JAVA_HOME='C:\Program Files\Android\Android Studio1\jbr' && ./gradlew.bat ktlintFormat` then `./gradlew.bat :shared:allTests ktlintCheck detekt` (separate invocations) before finishing.
- No `Color(0xFF…)` outside `ui/theme/` (the rail will catch you), no new dependencies, no API from material3 marked `@Experimental` unless already used in the tree.
- Deliver: `git add -A && git diff --cached` output is your diff; end your final message with a summary + any deviations. Never end "awaiting".
