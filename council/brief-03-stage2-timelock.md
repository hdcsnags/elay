Michael here, via Fable (lead orchestrator; build authority delegated). STAGE 2 — the signature feature: time-lock negotiation for a pair. Two blind lanes run this brief: the CONTRACT lane answers §A; the UX lane answers §B. Answer ONLY your lane's section (you know your lane from your seat's dispatch context; if unsure: codex seats = §A, agy seats = §B).

CONTEXT (both lanes). Read: `council/master-plan-v2.md` (pairs-first; week-one hook = propose→accept in seconds, dual-time everywhere; 1–3 candidate times per ADR-010 as amended), `ELAY-SPEC.md` §4 (state machine, negotiation rules, dual-time example), `adr/ADR-006` (time truth + recurrence policy), `adr/ADR-010` (per-participant commitments, durable delivery jobs, candidates amendment), `contracts/stage1-pairing.md` + `council/stage1-pairing-contract-sol.md` (pairs schema, realtime topics with reserved proposal events, projection RPC pattern, receipts style), `contracts/phase1-planner.md` (house wire rules), shipped migrations (esp. `20260912010000_stage1_pairing.sql`). Stage 2 scope: propose (1–3 candidates, creator-local composed, recipient sees own zone), respond (accept-one-candidate / decline / counter with new candidates), cancel, expire (deadline), complete; revision history append-only; per-participant commitments on acceptance; conflict indication against the responder's OWN ELAY blocks (busy-only, never details — ADR-007); realtime proposal events; NO calendar integration (Stage 4), NO push (FCM pinged), in-app + realtime only.

§A — CONTRACT (Sol's lane): write the buildable contract in house style:
1. Schema: `time_lock_proposals` (pair-scoped, creator, status enum per §4, response deadline, origin_tz, current_revision), `proposal_revisions` (append-only: revision n, author, 1–3 candidate slots with UTC instants + duration), `proposal_responses` (per member per revision: accept(candidate_idx)/decline/counter-pointer), `commitments` (ADR-010 shape; created atomically on acceptance; the accepted candidate becomes a real `time_blocks` row for EACH member — decide and commit: shared block rows per member vs one shared row + membership, argue from ADR-010 independence + existing time_blocks owner-only RLS), all CHECKs/indexes/RLS (participants = pair members only; base owner-only where applicable + projections where peers read).
2. RPCs + receipts (house pattern): `rpc_create_proposal`, `rpc_respond_proposal` (atomic winner selection on accept — both members' blocks + commitments + status transition in ONE transaction; decline; counter creates revision n+1 and re-opens), `rpc_cancel_proposal`, `rpc_complete_lock`, expiry handling (deadline check inside RPCs + a sweep function `fn_expire_proposals` callable by tests/cron), conflict-hint RPC for the composer (responder's busy windows, busy-only granularity).
3. Realtime: fill the reserved `pair.proposal_created.v1` / `pair.proposal_updated.v1` / `pair.commitment_changed.v1` payloads (sanitized: ids/status/version only — titles NEVER over broadcast per Stage 1 rule).
4. Frozen Kotlin surface: `ProposalRepository` (observe active/history, create, respond, cancel, complete), domain types incl. `Candidate`, `ProposalState`, dual-time rendering inputs (the CLIENT renders both zones from instants + zone ids — no server-formatted strings).
5. pgTAP adversarial list: non-participant denial; cross-pair proposal denial; stale-revision responses; double-accept race (one winner); accept-after-expire/cancel; counter depth; DST boundary candidates (Vancouver/Toronto fixtures per gate: spring-forward and fall-back — candidates AT the transition); commitment independence (one member's calendar-later-withdrawal never cancels the lock — Stage 4 concern but schema must allow); receipts replay per RPC.
6. Seat slicing (disjoint) + the 3 likeliest agent failure modes with guardrails.
End §A with JSON between BEGIN VERDICT/END VERDICT: {"lane":"contract","block_model":"per_member_rows|shared_row","expiry":"rpc_checks_plus_sweep|...","sections_ready":true|false,"seat_slices":[...],"failure_modes":[...],"confidence":0..1}

§B — UX SPEC (Gemini's lane): write the screen-and-copy spec the UI seat builds from:
1. Proposal composer (from Together and from Plan): 1–3 candidate pickers with LIVE dual-time preview per candidate ("7:00–8:00 PM for you · 10:00–11:00 PM for Alex" — day-crossover marked), deadline picker (sane defaults), the exact calm copy for every label/button/empty/error state.
2. Proposal card states (Together feed): incoming (candidates as tappable chips with both times, Accept-this-one / Suggest different / Can't), outgoing (waiting/deadline countdown), countered (what changed, highlighted), accepted (the winning time in both zones + "on both plans"), declined/expired/cancelled (calm, next-step-offering copy).
3. The dual-time visual rule (the signature): one consistent pattern for rendering my-time vs their-time everywhere (weight/size/order), including relative day markers ("tomorrow", "Sat") and the DST-weirdness case (offsets differing from usual — how the UI hints it without jargon).
4. Conflict hints in the composer/response ("You're busy 9–10 PM" — busy-only, never why), and how strongly they discourage vs inform (spec §2: inform, never nag).
5. Accessibility notes (chips, countdowns, dual-time readouts for screen readers).
End §B with JSON: {"lane":"ux","screens":["..."],"dual_time_rule":"...","copy_ready":true|false,"confidence":0..1}

Ground in the files (quote §s). Read-only; modify nothing. Each lane 1,200–1,800 words. Begin with the model you are running as.
