Michael here, via Fable (lead orchestrator; build authority delegated). STAGE 3 — no-install web RSVP: a pair member responds to a time-lock proposal from a browser via a signed expiring link, without opening the app. Two blind lanes: the SECURITY CONTRACT lane answers §A; the WEB UX lane answers §B. Answer ONLY your lane's section (your dispatch context says which you are; if unsure: the Claude/Opus seat = §A, the agy/Gemini seat = §B). Read-only; modify nothing. Begin with the model you are running as.

CONTEXT (both lanes). Read: `council/master-plan-v2.md` (Stage 3 row + ADD bullet: "signed expiring token → browser page with dual-time preview, accept/decline/counter; edge function + public response table"), `ELAY-SPEC.md` §4 (state machine, dual-time example), `contracts/stage2-timelock.md` + `council/stage2-timelock-sol.md` (the shipped negotiation contract: proposals/revisions/responses/commitments, receipts, expire-before-mutate, sanitized broadcasts), shipped migrations `20260912130000..170000` (esp. `rpc_respond_proposal`'s atomic accept and the conflict envelope shapes), `council/stage2-timelock-gemini.md` (the dual-time visual rule the web page must inherit), STATE.md newest entries (Stage 2 CLOSED record). Constraints: local Supabase only (Docker; Edge Functions run under `supabase functions serve`), pairs-only V1, the RECIPIENT IS ALWAYS the other pair member (a real account — web RSVP is a convenience channel, not a guest system), no push/FCM yet (link is surfaced in-app by the proposer to share, and later rides notifications), NO calendar integration.

§A — SECURITY CONTRACT (Claude lane): write the buildable contract in house style:
1. Token design: what a web-RSVP token asserts (proposal id, recipient user id, revision at mint?), signing scheme available in a Supabase Edge Function (HMAC vs JWT — pick, justify), TTL and its relation to the proposal's response_deadline, single-use vs multi-view semantics (viewing is read; responding is the mutation), revocation on: counter/new revision, cancel/expiry/acceptance, pair membership change (channel_generation lesson), re-mint rules. Threat-model the leaked link explicitly: what the page may reveal to a token holder (title? both zones' times? the proposer's identity?) and what it must never reveal; rate limits and enumeration resistance (token length/entropy, constant-time compare, no oracle responses).
2. Server surface: the minting RPC (who may mint — either member? proposer only?), receipts pattern, and the Edge Function endpoints (GET render-data, POST respond) — how the function authenticates to Postgres (service role + explicit recipient check from the token, never trusting client input), how a web response reuses `rpc_respond_proposal`'s atomicity/idempotency (operation_id derivation from token+action), and the conflict/expired/stale-revision responses the page must handle (map the Stage-2 conflict envelope shapes). Decide: does web counter mint a new token pair? Does accepting from web create the same per-member shared_lock rows (it must — argue from the shipped contract).
3. Schema deltas: `rsvp_tokens` table (hash-at-rest? plaintext never stored — argue), indexes, RLS (service-role only?), pgTAP adversarial list: forged/expired/revoked/cross-proposal/cross-recipient tokens, replayed responses, stale-revision from web, token minted then proposal countered, membership-left recipient, rate-limit behavior.
4. Seat slicing (disjoint files) + the 3 likeliest agent failure modes with guardrails.
End §A with JSON between BEGIN VERDICT/END VERDICT: {"lane":"security-contract","token_scheme":"hmac|jwt","stored":"hash|plaintext","sections_ready":true|false,"seat_slices":[...],"failure_modes":[...],"confidence":0..1}

§B — WEB PAGE UX (Gemini lane): write the page-and-copy spec the web seat builds from:
1. The response page (mobile-first browser, no install): layout for ONE proposal — title, proposer, 1–3 candidate chips each with the dual-time rule (recipient's zone primary — the token knows the recipient; proposer's secondary; relative-day badges; DST caption rule inherited from your Stage 2 §B), deadline countdown, Accept-this-one / Suggest different / Can't — exact calm copy for every state.
2. Terminal/edge states: token expired, proposal already accepted/declined/cancelled/expired, countered-since-mint (stale link → what does the page say and offer?), network failure, double-submit; success confirmations ("On both plans" moment) — calm copy for each, no dead ends (each state offers "open the app" as the next step).
3. Counter-from-web: how far the composer goes in a browser (full 1–3 candidate picker vs a constrained "suggest one different time" — recommend and justify against week-one simplicity), dual-time preview requirements.
4. Anti-confusion: the page must make clear WHO is responding (recipient's name from the token) and that this is their pair's private negotiation; what it shows if opened by someone else with the leaked link (defer to §A's ruling — spec the safe-rendering variant).
5. Accessibility + no-JS/slow-network degradation notes.
End §B with JSON: {"lane":"web-ux","page_states":["..."],"counter_scope":"full|constrained","copy_ready":true|false,"confidence":0..1}

Ground in the files (quote §s). Each lane 1,000–1,600 words.
