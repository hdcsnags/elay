Michael here, via Fable (lead orchestrator; build authority delegated). STAGE 3 PRE-GATE VERIFICATION — web RSVP. Adversarial verification before closure. Read-only; modify nothing. Begin with the model you are running as. STATIC pass: do NOT run Gradle/emulators; you MAY run `npx supabase test db --local` (and nothing else against the stack). Deliver the COMPLETE numbered findings + verdict JSON in your single final message — never end "awaiting" anything.

CLAIMS UNDER TEST (all the lead's — attack them):
1. Contract compliance: `contracts/stage3-web-rsvp.md` + `council/stage3-web-rsvp-security-opus.md` §A vs the shipped `supabase/migrations/20260912190000_stage3_web_rsvp.sql` + pgTAP `0022`... correction: suite is `supabase/tests/0021_stage3_web_rsvp_test.sql` (claimed 511/511 total). Attack: token scheme (HMAC derivation, base32 codec correctness — it is HAND-WRITTEN numeric-arithmetic base conversion, never verified beyond execution in pgTAP; look for bias/short-token edge cases), hash-at-rest (grep for any plaintext persistence), derived-revocation predicate completeness, rate limiting (attempt logging unconditional? limits enforced before MAC work?), the mint author check, and the wrapper's receipt-replay-before-liveness-gate fix (a lead patch at merge — is the cross-action 22023 raise correct and unreachable states handled?).
2. The wrapper's transaction-local impersonation (`set_config('request.jwt.claims', …, true)` saved/restored): any leak path (exception between set and restore — what does an EXCEPTION in rpc_respond_proposal do to the restore? Does the transaction abort make it moot?), and whether the service-role GET/POST surface can be reached by anon/authenticated (grants).
3. The Edge Function `supabase/functions/rsvp/index.ts`: token-is-only-identifier (body ids ignored), headers on EVERY response path, token never logged, the reconciled render-data mapping (outcome=ok/state split), p_client_ip from x-forwarded-for (spoofable? acceptable for a rate-limit hint?), no-JS form correctness, and the §B copy fidelity vs `council/stage3-web-rsvp-gemini.md`.
4. Client (B5/C4 merges): `RsvpDtos.kt` fixture round-trip vs `contracts/fixtures/rsvp-mint.json`; the mint path mapping inside the failure barrier; C4's share sheet disclosure copy server-driven; the affordance only on outgoing cards; `RSVP_LINK_BASE` localhost TODO acceptable for this stage?
5. Evidence honesty: STATE.md's two newest entries vs `research/shots/stage3-e2e-01..03.png` — is the "full circle" claim (app-minted link → browser accept → realtime flip) actually evidenced?
6. Regression: could 20260912190000 break suites 0001–0020 or the Stage 1/2 client paths? Fresh-reset ordering (the live DB got two lead function patches — composite INTO fix, substr fix, receipt-replay fix — verify the FILE contains all three so a fresh reset converges; run the pgTAP suite yourself to confirm 511/511).

DELIVERABLE: numbered findings tagged VERIFIED-OK / BUG (repro) / RISK (exploit sketch) / UNVERIFIED (what you'd need), file:line quotes, then JSON between BEGIN VERDICT/END VERDICT:
{"lane":"pregate-verification","stage3_ready_to_close":true|false,"blocking_findings":[...],"nonblocking_findings":[...],"confidence":0..1}
