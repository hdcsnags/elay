Coder seat W — ELAY Stage 0 wiring: DI graph, sign-in, real repository into the app, 4-tab reshape. This seat carries an EXPLICIT concierge grant on normally concierge-owned files (App.kt, androidApp sources, ui factories). Your diff is collected for review; commit nothing.

READ FIRST: `council/master-plan-v2.md` (the pivot — 4 tabs: Today · Plan · Together · Inbox), `contracts/phase1-planner.md` §5, the merged pieces you are wiring: `data/repository/LocalFirstPlannerRepository.kt` (+ its kdoc on staging/enqueue ownership), `data/remote/impl/` (gateways — constructor takes a SupabaseClient), `sync/impl/OutboxSyncCoordinator.kt` (check its constructor for what it needs), `data/local/DatabaseBuilder.android.kt|ios.kt` (per-account), `ui/*/…ViewModel.kt` factories (C1 wired them to `sharedFakePlannerRepository`), `androidApp/build.gradle.kts` (BuildConfig.SUPABASE_URL / SUPABASE_ANON_KEY already exist).

BUILD:
1. `shared/src/commonMain/kotlin/dev/elay/di/AppGraph.kt` — manual DI, no framework: constructed with (supabaseUrl, supabaseAnonKey, databaseBuilderFactory: (accountKey: String) -> RoomDatabase.Builder<ElayDatabase>, appScope: CoroutineScope). Builds: SupabaseClient (`createSupabaseClient` installing Auth, Postgrest, Realtime), `SupabaseAuthGateway`. Exposes a `UserSession` layer: when `AuthGateway.session` becomes SignedIn(userId) → lazily build the per-account ElayDatabase, `SupabaseDataGateway`, `OutboxSyncCoordinator`, `LocalFirstPlannerRepository`; on SignedOut → close/clear the user scope. Expose: `authGateway`, `sessionFlow`, and `userScope: Flow<UserGraph?>` (UserGraph = repository + syncCoordinator).
2. Replay lifecycle (Phase 1 simple): on user-scope creation, `appScope.launch { syncCoordinator.replayOnce() }`; repository writes already enqueue — after each enqueue the coordinator does NOT self-trigger, so wrap the repository in a thin decorator (in di/) that calls `replayOnce()` fire-and-forget after each write, OR launch a periodic replay (every 30s) in the user scope — pick one, justify in the report.
3. `shared/src/commonMain/kotlin/dev/elay/ui/auth/SignInScreen.kt` + ViewModel — email + password fields, Sign in AND Create account actions (local Supabase auto-confirms signups), calm error copy (no red banners — spec §2), loading state. Plain testable ViewModel like C1's.
4. `App.kt` rewrite: takes the AppGraph; renders SignInScreen when SignedOut, the tab shell when SignedIn. Bar shows FOUR surfaces per master-plan-v2: Today, Plan, Together, Inbox (keep the Surface enum + routes file intact — filter the bar list; Goals/Review routes stay registered but unlisted). Together = existing placeholder.
5. Swap the fakes: change C1's `remember*ViewModel` factories to take the real `PlannerRepository` from the graph (parameter or CompositionLocal — your choice, justify). `sharedFakePlannerRepository` stays in the tree for tests/previews only.
6. Platform entries: `androidApp` — `ElayApp` builds the AppGraph with BuildConfig values + `databaseBuilder(context, key)`; MainActivity passes it to App(). `iosMain` — MainViewController builds the graph with iOS databaseBuilder; supabase url/key via an `expect/actual PlatformConfig` (iOS actual can hardcode the local defaults with a TODO — iOS runtime is CI-only).
7. Tests (commonTest): SignInViewModel (success/failure/loading via a fake AuthGateway); AppGraph user-scope lifecycle (fake gateways: SignedIn builds a UserGraph, SignedOut tears it down); the replay-trigger decorator if you chose it.

TOUCH ONLY: `shared/src/commonMain/kotlin/dev/elay/di/**` (new), `ui/auth/**` (new), `App.kt`, the `remember*ViewModel` factory functions inside `ui/today|inbox|plan` (factories only — screen bodies stay), `androidApp/src/**`, `shared/src/iosMain/kotlin/dev/elay/**`, commonTest equivalents. Never: gradle files, contracts, data/, sync/, domain/, .github/.

VERIFY (SYNCHRONOUS, same call, no background/monitors): `export JAVA_HOME='C:\Program Files\Android\Android Studio1\jbr'` then `./gradlew.bat :shared:allTests :androidApp:assembleDebug ktlintCheck detekt --console=plain --no-daemon` to BUILD SUCCESSFUL. Traps: `runCurrent()` not `advanceUntilIdle`; `kotlin.time.Clock`; max line 120; supabase-kt 3.8 `session.user` nullable; do NOT construct network calls in tests.

Report: file list · replay-trigger + factory-injection choices with reasons · verification + final lines · test count (baseline 87) + delta · UNVERIFIED (expect: live sign-in against the running stack and the emulator E2E — both the concierge's). Begin with the model you are running as.
