Coder seat W2 — ELAY Gate 1 finisher: schedule UI, hydration, sign-out, session-restore polish. Integration-shaped seat with explicit grants (like seat W). Commit nothing; your diff is reviewed.

READ FIRST: `STATE.md` top entry (the live-E2E findings — your work items come from its "Honest Gate 1 remainder" list), `council/master-plan-v2.md`, the merged `di/` (AppGraph, UserGraph, UserSessionGraph, LocalPlannerRepository/LocalCurrentUserId), `ui/` surfaces, `data/remote/Gateways.kt` (`fetchSince`), `data/remote/dto/` (DTO↔domain mappers), `domain/repository/PlannerRepository.kt`, `sync/impl/OutboxSyncCoordinator.kt`.

BUILD:
1. **Schedule UI**: on PlanScreen add an "Add block" affordance (M3 FAB or header button) opening a sheet/dialog: optional link to an unscheduled task (dropdown from `observeUnscheduledTasks`), title (prefilled from the task), date = the Plan screen's shown day, start time + duration (M3 TimePicker or simple pickers), Save → `repository.upsertBlock(TimeBlock(... originTz = TimeZone.currentSystemDefault(), version = 0, id = Uuid.random()))`. All time math via kotlinx-datetime (ADR-006). Also give the unscheduled-task rail a "Schedule" button per task opening the same sheet prefilled.
2. **Hydration**: new `dev.elay.sync.impl.ServerHydrator` (grant): on construction/user-scope creation, for each aggregate call `DataGateway.fetchSince(aggregate, 0)`, map DTO→domain→entity, upsert into Room with `syncStatus = SYNCED` — SKIPPING any local row whose syncStatus is PENDING/CONFLICT (never clobber unsynced local edits). Wire it in `AppGraph`'s user-scope build (after Room, before replay). Errors: log via println + give up quietly (retry next app start) — hydration must never block startup.
3. **Settings + sign-out**: minimal SettingsScreen (route exists, currently unregistered content): shows the signed-in email if available from the session (else the user id), Sign out button → `authGateway.signOut()` (UserSessionGraph already tears down). Reachable via a gear/settings icon in Today's header row. Register `SettingsRoute` in App.kt's NavHost.
4. **Session-restore loading state**: App currently renders SignInScreen while the session restores (flash). Introduce an explicit initial/loading branch: if the auth gateway can express "restoring" (check what `SupabaseAuthGateway` maps `sessionStatus.Initializing` to — if it collapses to SignedOut, add a `SessionState.Restoring`-like handling INSIDE di/ui without changing the frozen `SessionState` — e.g., a small `sessionReady: Flow<Boolean>` on AppGraph driven by the SDK's status), and show a centered spinner until resolved.
5. **Auth ops off the composable scope**: sign-in/sign-up coroutines currently launch on `rememberCoroutineScope` → ForgottenCoroutineScopeException when navigation leaves the screen mid-op. Provide the AppGraph's appScope to the sign-in factory (new `LocalAppScope` CompositionLocal in di/, provided in App.kt) so auth operations survive the screen.
6. **Tests** (commonTest): ServerHydrator (fake gateway + fake DAOs: hydrates SYNCED rows, never clobbers PENDING/CONFLICT, swallows failures); schedule-sheet ViewModel logic if you extract one (block construction: correct instants for chosen local time/zone, duration bounds 1min..24h); sign-out lifecycle via existing UserSessionGraph fakes.

TOUCH ONLY: `shared/src/commonMain/kotlin/dev/elay/ui/plan/**`, `ui/settings/**` (new), `ui/auth/**`, `ui/today/TodayScreen.kt` (header gear only), `App.kt`, `di/**`, `sync/impl/ServerHydrator.kt` (new only — do NOT edit OutboxSyncCoordinator), commonTest equivalents. Never: gradle files, contracts, data/local (read-only), data/remote (read-only), domain/, .github/.

VERIFY (SYNCHRONOUS, no background runs/monitors): `export JAVA_HOME='C:\Program Files\Android\Android Studio1\jbr'` then `./gradlew.bat :shared:allTests :androidApp:assembleDebug ktlintCheck detekt --console=plain --no-daemon` to BUILD SUCCESSFUL. Traps: `runCurrent()` not `advanceUntilIdle`; `kotlin.time.Clock`; `kotlin.uuid.Uuid.random()` with `@OptIn(ExperimentalUuidApi)`; max line 120; ktlintFormat for nits; M3 TimePicker may need `@OptIn(ExperimentalMaterial3Api::class)`.

Report: file list · design choices (sheet shape, restoring-state mechanism) · verification + final lines · test count (baseline 100) + delta · UNVERIFIED (live E2E is the concierge's). Begin with the model you are running as.
