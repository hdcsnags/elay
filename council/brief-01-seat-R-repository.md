Coder seat R — ELAY Phase 1: the local-first PlannerRepository implementation. You have WRITE access in your own clone; your diff is collected for concierge review.

READ FIRST: `contracts/phase1-planner.md` §3 (outbox semantics), §5 (the frozen `PlannerRepository` you are implementing); `adr/ADR-003` (local-first: reads observe Room; writes stage optimistically and render pending until acknowledged); the merged layers you compose — `data/local/` (entities, DAOs, mappers, `PlannerWriteDao`), `sync/SyncContracts.kt` (`MutationCommand`, `SyncCoordinator` — you ENQUEUE, never dispatch), `domain/model/PlannerModels.kt`.

BUILD:
1. `shared/src/commonMain/kotlin/dev/elay/data/repository/LocalFirstPlannerRepository.kt` implementing `PlannerRepository`:
   - Reads: DAO Flows mapped to domain via the existing mappers (observeToday takes Instants → epoch ms; etc.).
   - Writes (upsert/delete/clarify/dismiss): in ONE Room transaction — optimistic local write with `syncStatus = PENDING` + outbox enqueue row `(operationId = new uuid, aggregate, aggregateId, expectedVersion = current local version or 0 for new, type, payloadJson = the wire-format row per contract §4 — reuse seat B2's DTOs in `data/remote/dto/` for serialization)`. After the transaction, call `SyncCoordinator.enqueue`-equivalent semantics: the coordinator reads the outbox, so a `suspend fun nudge()`-style call is NOT yours to add — construct the repository with the `SyncCoordinator` and call its `enqueue(command)` (it persists via OutboxDao internally per its contract; check the merged `OutboxSyncCoordinator` to see whether enqueue also writes the outbox row — DO NOT double-enqueue; reconcile so exactly one outbox row exists per mutation, and say in your report which side owns the write).
   - Deletes: optimistic local removal + Delete command enqueue. `clarifyCapture`: capture → Parsed + clarified_task_id locally + upsert command for the capture (the new Task itself is created by the caller via upsertTask — keep the two mutations separate commands).
2. You MAY add to `data/local/`: new @Transaction methods on `PlannerWriteDao` (e.g. `stageUpsertGoalWithOutbox(entity, outbox)`) and small DAO additions needed for staging — this grant is explicit for THIS seat (previous seats' read-only rule on data/local does not apply to you). You may NOT change existing method signatures, entities' columns, or the database version.
3. Tests (`commonTest`): against hand-written fakes of the DAOs/coordinator (no live sqlite on this host — see existing test comments): every write produces exactly one outbox row with correct expectedVersion/wire payload; optimistic state renders immediately (PENDING); reads map correctly (reuse patterns from existing mapper tests); clarify produces the right capture mutation; delete removes locally and enqueues.

TOUCH ONLY: `shared/src/commonMain/kotlin/dev/elay/data/repository/`, `shared/src/commonMain/kotlin/dev/elay/data/local/` (additive only, per the grant above), and commonTest equivalents. Never: gradle files, domain/, data/remote/impl/, sync/impl/, ui/, contracts/, .github/.

VERIFY (SYNCHRONOUS, same tool call, no background runs/monitors): `export JAVA_HOME='C:\Program Files\Android\Android Studio1\jbr'` then `./gradlew.bat :shared:allTests :androidApp:assembleDebug ktlintCheck detekt --console=plain --no-daemon` — iterate to BUILD SUCCESSFUL. Known traps: coroutines 1.11 `advanceUntilIdle` doesn't drain backgroundScope (use `runCurrent()`); kotlinx-datetime 0.8 has no `Clock` (use `kotlin.time.Clock`); max line 120; `ktlintFormat` for nits.

Report: file list · the enqueue-ownership reconciliation you chose · verification commands + final lines · executed-test count (baseline 73) and delta · UNVERIFIED items. Begin with the model you are running as.
