<?xml version="1.0" encoding="utf-8"?>
<!-- MASVS STORAGE-2 (Stage 5 hardening contract item 1): the per-account Room DBs and the
     supabase-kt session store live in app-private storage; none of it may ride cloud backup
     or device transfer. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
