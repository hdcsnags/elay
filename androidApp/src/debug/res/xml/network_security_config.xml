<?xml version="1.0" encoding="utf-8"?>
<!-- Debug resource OVERRIDE of main's TLS-only config: once a networkSecurityConfig exists,
     the manifest overlay's usesCleartextTraffic flag is ignored (API 24+), so the local
     stack's cleartext allowance must live HERE. Loopback + emulator-host only. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
